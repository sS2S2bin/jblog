<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog">

	
	<insert id="insert" parameterType="string">
		<![CDATA[
		insert
		  into blog
		values ( #{id }, #{id }, '/assets/images/logo.jpg')
		]]>
	</insert>
	
	
	<select id="getContent" parameterType="map" resultType="postvo">
		<![CDATA[
		select p.title as title, p.contents as contents, p.reg_date as regDate, p.category_no as categoryNo
		from category c, blog b, post p
		where c.blog_id = b.id
		and p.category_no = c.no 
		and b.id = #{id} 
		and p.category_no = #{categoryNo}
		and p.no = #{postNo}
		]]>
	</select>
	
	<select id="getBlogContent" parameterType="string" resultType="blogvo">
	<![CDATA[
		select title, log 
		from blog
		where id=#{id}
		]]>
	</select>
	
	<select id="getCategoryList" parameterType="string" resultType="categoryvo">
		<![CDATA[
		select no, name from category
		where blog_id=#{id}
		]]>
	</select>
	
	<select id="getMinCategory" parameterType="string" resultType="long">
		<![CDATA[
		select min(no) from category
		where blog_id=#{id}
		]]>
	</select>
	
	<select id="getPostList" parameterType="long" resultType="postvo">
		<![CDATA[
		select no,title,contents, reg_date, category_no from post
		where category_no = #{category_no}
		]]>
	</select>
	
	<select id="getMinPost" parameterType="long" resultType="long">
		<![CDATA[
		select min(no) from post
		where category_no=#{category_no}
		]]>
	</select>
	
	<update id="update" parameterType="uservo">
		<choose>
			<when test='password == ""'>
				<![CDATA[
				update user
				   set name=#{name },
				       gender=#{gender }
				 where no=#{no }
				]]>
			</when>
			<otherwise>
				<![CDATA[
				update user
				   set name=#{name },
				       password=password(#{password }),
				       gender=#{gender }
				 where no=#{no }
				]]>
			</otherwise>
		</choose>
	</update>
</mapper>